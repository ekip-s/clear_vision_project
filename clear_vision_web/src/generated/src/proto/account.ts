// Code generated by protoc-gen-ts_proto. DO NOT EDIT.
// versions:
//   protoc-gen-ts_proto  v2.8.3
//   protoc               v6.33.0
// source: src/proto/account.proto

/* eslint-disable */
import { BinaryReader, BinaryWriter } from "@bufbuild/protobuf/wire";
import { Empty } from "../../google/protobuf/empty";

export const protobufPackage = "account";

export interface CreateAccountRequest {
  userId: number;
  accountType: string;
  initialBalance: number;
}

export interface GetUserAccountsRequest {
  userId: number;
}

export interface Account {
  accountId: number;
  accountType: string;
  balance: number;
}

export interface UserAccountsResponse {
  accounts: Account[];
}

function createBaseCreateAccountRequest(): CreateAccountRequest {
  return { userId: 0, accountType: "", initialBalance: 0 };
}

export const CreateAccountRequest: MessageFns<CreateAccountRequest> = {
  encode(message: CreateAccountRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    if (message.accountType !== "") {
      writer.uint32(18).string(message.accountType);
    }
    if (message.initialBalance !== 0) {
      writer.uint32(25).double(message.initialBalance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): CreateAccountRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseCreateAccountRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.initialBalance = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): CreateAccountRequest {
    return {
      userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0,
      accountType: isSet(object.accountType) ? globalThis.String(object.accountType) : "",
      initialBalance: isSet(object.initialBalance) ? globalThis.Number(object.initialBalance) : 0,
    };
  },

  toJSON(message: CreateAccountRequest): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    if (message.accountType !== "") {
      obj.accountType = message.accountType;
    }
    if (message.initialBalance !== 0) {
      obj.initialBalance = message.initialBalance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<CreateAccountRequest>, I>>(base?: I): CreateAccountRequest {
    return CreateAccountRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<CreateAccountRequest>, I>>(object: I): CreateAccountRequest {
    const message = createBaseCreateAccountRequest();
    message.userId = object.userId ?? 0;
    message.accountType = object.accountType ?? "";
    message.initialBalance = object.initialBalance ?? 0;
    return message;
  },
};

function createBaseGetUserAccountsRequest(): GetUserAccountsRequest {
  return { userId: 0 };
}

export const GetUserAccountsRequest: MessageFns<GetUserAccountsRequest> = {
  encode(message: GetUserAccountsRequest, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.userId !== 0) {
      writer.uint32(8).int32(message.userId);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): GetUserAccountsRequest {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseGetUserAccountsRequest();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.userId = reader.int32();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): GetUserAccountsRequest {
    return { userId: isSet(object.userId) ? globalThis.Number(object.userId) : 0 };
  },

  toJSON(message: GetUserAccountsRequest): unknown {
    const obj: any = {};
    if (message.userId !== 0) {
      obj.userId = Math.round(message.userId);
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<GetUserAccountsRequest>, I>>(base?: I): GetUserAccountsRequest {
    return GetUserAccountsRequest.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<GetUserAccountsRequest>, I>>(object: I): GetUserAccountsRequest {
    const message = createBaseGetUserAccountsRequest();
    message.userId = object.userId ?? 0;
    return message;
  },
};

function createBaseAccount(): Account {
  return { accountId: 0, accountType: "", balance: 0 };
}

export const Account: MessageFns<Account> = {
  encode(message: Account, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    if (message.accountId !== 0) {
      writer.uint32(8).int32(message.accountId);
    }
    if (message.accountType !== "") {
      writer.uint32(18).string(message.accountType);
    }
    if (message.balance !== 0) {
      writer.uint32(25).double(message.balance);
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): Account {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseAccount();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 8) {
            break;
          }

          message.accountId = reader.int32();
          continue;
        }
        case 2: {
          if (tag !== 18) {
            break;
          }

          message.accountType = reader.string();
          continue;
        }
        case 3: {
          if (tag !== 25) {
            break;
          }

          message.balance = reader.double();
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): Account {
    return {
      accountId: isSet(object.accountId) ? globalThis.Number(object.accountId) : 0,
      accountType: isSet(object.accountType) ? globalThis.String(object.accountType) : "",
      balance: isSet(object.balance) ? globalThis.Number(object.balance) : 0,
    };
  },

  toJSON(message: Account): unknown {
    const obj: any = {};
    if (message.accountId !== 0) {
      obj.accountId = Math.round(message.accountId);
    }
    if (message.accountType !== "") {
      obj.accountType = message.accountType;
    }
    if (message.balance !== 0) {
      obj.balance = message.balance;
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<Account>, I>>(base?: I): Account {
    return Account.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<Account>, I>>(object: I): Account {
    const message = createBaseAccount();
    message.accountId = object.accountId ?? 0;
    message.accountType = object.accountType ?? "";
    message.balance = object.balance ?? 0;
    return message;
  },
};

function createBaseUserAccountsResponse(): UserAccountsResponse {
  return { accounts: [] };
}

export const UserAccountsResponse: MessageFns<UserAccountsResponse> = {
  encode(message: UserAccountsResponse, writer: BinaryWriter = new BinaryWriter()): BinaryWriter {
    for (const v of message.accounts) {
      Account.encode(v!, writer.uint32(10).fork()).join();
    }
    return writer;
  },

  decode(input: BinaryReader | Uint8Array, length?: number): UserAccountsResponse {
    const reader = input instanceof BinaryReader ? input : new BinaryReader(input);
    const end = length === undefined ? reader.len : reader.pos + length;
    const message = createBaseUserAccountsResponse();
    while (reader.pos < end) {
      const tag = reader.uint32();
      switch (tag >>> 3) {
        case 1: {
          if (tag !== 10) {
            break;
          }

          message.accounts.push(Account.decode(reader, reader.uint32()));
          continue;
        }
      }
      if ((tag & 7) === 4 || tag === 0) {
        break;
      }
      reader.skip(tag & 7);
    }
    return message;
  },

  fromJSON(object: any): UserAccountsResponse {
    return {
      accounts: globalThis.Array.isArray(object?.accounts) ? object.accounts.map((e: any) => Account.fromJSON(e)) : [],
    };
  },

  toJSON(message: UserAccountsResponse): unknown {
    const obj: any = {};
    if (message.accounts?.length) {
      obj.accounts = message.accounts.map((e) => Account.toJSON(e));
    }
    return obj;
  },

  create<I extends Exact<DeepPartial<UserAccountsResponse>, I>>(base?: I): UserAccountsResponse {
    return UserAccountsResponse.fromPartial(base ?? ({} as any));
  },
  fromPartial<I extends Exact<DeepPartial<UserAccountsResponse>, I>>(object: I): UserAccountsResponse {
    const message = createBaseUserAccountsResponse();
    message.accounts = object.accounts?.map((e) => Account.fromPartial(e)) || [];
    return message;
  },
};

export interface AccountService {
  /** Метод для создания аккаунта, принимает параметры создания, возвращает пустой ответ */
  CreateAccount(request: CreateAccountRequest): Promise<Empty>;
  /** Метод для получения счетов пользователя */
  GetUserAccounts(request: GetUserAccountsRequest): Promise<UserAccountsResponse>;
}

export const AccountServiceServiceName = "account.AccountService";
export class AccountServiceClientImpl implements AccountService {
  private readonly rpc: Rpc;
  private readonly service: string;
  constructor(rpc: Rpc, opts?: { service?: string }) {
    this.service = opts?.service || AccountServiceServiceName;
    this.rpc = rpc;
    this.CreateAccount = this.CreateAccount.bind(this);
    this.GetUserAccounts = this.GetUserAccounts.bind(this);
  }
  CreateAccount(request: CreateAccountRequest): Promise<Empty> {
    const data = CreateAccountRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "CreateAccount", data);
    return promise.then((data) => Empty.decode(new BinaryReader(data)));
  }

  GetUserAccounts(request: GetUserAccountsRequest): Promise<UserAccountsResponse> {
    const data = GetUserAccountsRequest.encode(request).finish();
    const promise = this.rpc.request(this.service, "GetUserAccounts", data);
    return promise.then((data) => UserAccountsResponse.decode(new BinaryReader(data)));
  }
}

interface Rpc {
  request(service: string, method: string, data: Uint8Array): Promise<Uint8Array>;
}

type Builtin = Date | Function | Uint8Array | string | number | boolean | undefined;

export type DeepPartial<T> = T extends Builtin ? T
  : T extends globalThis.Array<infer U> ? globalThis.Array<DeepPartial<U>>
  : T extends ReadonlyArray<infer U> ? ReadonlyArray<DeepPartial<U>>
  : T extends {} ? { [K in keyof T]?: DeepPartial<T[K]> }
  : Partial<T>;

type KeysOfUnion<T> = T extends T ? keyof T : never;
export type Exact<P, I extends P> = P extends Builtin ? P
  : P & { [K in keyof P]: Exact<P[K], I[K]> } & { [K in Exclude<keyof I, KeysOfUnion<P>>]: never };

function isSet(value: any): boolean {
  return value !== null && value !== undefined;
}

export interface MessageFns<T> {
  encode(message: T, writer?: BinaryWriter): BinaryWriter;
  decode(input: BinaryReader | Uint8Array, length?: number): T;
  fromJSON(object: any): T;
  toJSON(message: T): unknown;
  create<I extends Exact<DeepPartial<T>, I>>(base?: I): T;
  fromPartial<I extends Exact<DeepPartial<T>, I>>(object: I): T;
}
